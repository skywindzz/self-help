import React from 'react'

const NewAccount = props => {
     {/* New account page display a modal which requires name, email and password from user
     you'll also use some kind of ID generator for user account for furture use */}
    return (
        <div css={{ marginRight: '2em' }}>Sign up</div>
    )
}

export default NewAccount;